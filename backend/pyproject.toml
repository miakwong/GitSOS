# Auto pytest running setting
addopts = "--cov=app --cov-report=term-missing --cov-report=html"
testpaths = ["tests"]
python_files = "test_*.py"
asyncio_mode = "auto"

[tool.coverage.run]
source = ["app"]
# Ignore unrelated files
omit = ["tests/*", "**/__init__.py", "app/main.py"]